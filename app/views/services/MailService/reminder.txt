&{'hello'} ${user.nickname}!

&{'mails.reminder.header'}

-----------------------------------------------------------
&{'mails.reminder.tips'}
-----------------------------------------------------------

#{list items:games, as:'game'}
&{game.homeTeam?.name} : &{game.awayTeam?.name} / &{'mails.tips.due'}: ${game.tippEnding.formatted()}
#{/list}
#{else}
&{'notips'}
#{/else}

-----------------------------------------------------------
&{'mails.reminder.extratips'}
-----------------------------------------------------------

#{list items:extras, as:'extra'}
&{extra.question} / &{'mails.tips.due'}s: ${extra.ending.formatted()}
#{/list}
#{else}
&{'noextratips'}
#{/else}

---
&{'mails.reminder.deactivate'}